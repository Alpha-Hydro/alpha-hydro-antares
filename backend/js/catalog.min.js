var categoryHeight = (function () {
    function categoryHeight(classItem) {
        this.items = [].slice.call(document.querySelectorAll(classItem));
        this.height = 200;
        this._init();
    }
    categoryHeight.prototype._init = function () {
        var _this = this;
        this.items.forEach(function (element) {
            if (element.offsetHeight > _this.height) {
                _this.height = element.offsetHeight;
            }
            element.style.height = _this.height + 'px';
        });
    };
    return categoryHeight;
}());
new categoryHeight('.categories-list-item');
new categoryHeight('.categories-item');
//# sourceMappingURL=catalog.min.js.map