<?php
    $this->title = 'Каталог продукции';
    $this->headTitle($this->title);
    $this->sidebar_headers = false;
?>


<div class="col-md-12">
    <?php foreach ($this->entries as $entry): ?>
        <div class="col-md-3 text-center categories-item">
            <div class="text-center <?=($entry->active != 0)?'':'disabled';?>">
                <a href="<?=$this->url(array('fullPath'=>$entry->fullPath),'catalog',true,false) ?>">
                    <img src="<?=$entry->uploadPath.$entry->image;?>" alt="<?=$entry->name?>"/>
                    <div><?=$entry->name?></div>
                </a>
            </div>
            <?php if($this->auth):?>
                <div class="itemButtonsComponent"
                     data-id="<?=$entry->id?>"
                     data-controller="categories"
                     data-active="<?=($entry->active != 0)?true:false?>"
                ></div>
            <?php endif;?>
        </div>
    <?php endforeach; ?>
    <?php if($this->auth):?>
        <div class="col-md-3 text-center">
            <!--<div class="text-center mt2">
                <button
                    class="btn btn-default btn-lg col-md-10 col-md-offset-1 add-item mb1"
                    data-controller="categories">
                    
                    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                </button>
                <div class="mt1">Добавить категорию</div>
            </div>-->

            <a href="#" class="categories-list-item z-depth-1">
                <p class="categories-list-item-name">Добавить категорию</p>
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
            </a>
        </div>
    <?php endif;?>
</div>