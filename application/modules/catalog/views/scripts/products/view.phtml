<?php
$this->headTitle($this->title .'. '. $this->secondaryHeader);
$this->meta_title = $this->title .'. '. $this->secondaryHeader;
$this->breadcrumbs = true;
$this->sidebar_headers = true;
$this->product_id = $this->product->id;
?>

<div class="col-xs-6 col-md-3">
    <div class="product-images">
        <div class="thumbnail">
            <img src="/files/images/product/<?=$this->product->image;?>" alt="<?=$this->product->name?>"/>
        </div>
        <?php if($this->draftImage):?>
            <div class="thumbnail">
                <img src="/files/images/product/<?=$this->draftImage;?>" alt="<?=$this->product->name?>"/>
            </div>
        <?php endif;?>
    </div>
</div>
<div class="col-xs-6 col-md-9">
    <?php if($this->productProperty):?>
        <h4>Свойства</h4>
        <table class="table table-striped table-condensed product-property">
            <?php foreach ($this->productProperty as $property):?>
                <tr>
                    <th class="col-md-4"><?= $property->name ?></th>
                    <td><?= $property->value ?></td>
                </tr>
            <?php endforeach;?>
        </table>
    <?php endif;?>
</div>

    <div class="col-md-12">
        <div class="footnote">
            <?php if($this->product->description != ''):?>
                <h4>Примечание</h4>
                <?=$this->product->description?>
            <?php endif;?>
        </div>
    </div>
<?php if($this->modifications):?>
    <div class="col-md-12">
        <!--<h4>Модификации и размеры</h4>-->
        <table class="table table-striped modifications">
            <thead>
            <tr>
                <td>Название</td>
                <?php foreach ($this->subproductProperty as $propertyName):?>
                    <td><?=$propertyName->name?></td>
                <?php endforeach;?>
            </tr>
            </thead>
            <tbody>
            <?php foreach ($this->modificationsTableValues as $modification):?>
                <tr>
                    <?php foreach ($modification as $value):?>
                        <td><?=$value ?></td>
                    <?php endforeach;?>
                </tr>
            <?php endforeach;?>
            </tbody>
        </table>
    </div>
<?php endif;?>

